# OpenAI 兼容中转站示例配置
# 二选一即可：API_KEY / OPENAI_API_KEY
API_KEY=sk-your-relay-key

# 二选一即可：BASE_URL / OPENAI_BASE_URL
# 如果你只填域名，后端会自动补成 /v1
BASE_URL=https://your-relay.example.com/v1

# 可按你的中转站支持情况修改默认模型
# 前端下拉里填写的模型名，也需要是中转站支持的模型名
